{{#each errors}}
    <div class="alert alert-danger">{{tip}}</div>
{{else}}

{{/each}}
<div calss="card card-body">
    <h3>想学的课程</h3>
    <form action="/ideas" method="post">
        <div class="form-group">
            <label for="title">标题</label>
            <input type="text" class="form-control" name="title" value="{{tipTitle}}">
        </div>
        <div class="form-group">
            <label for="details">详情</label>
            <input type="text" class="form-control" name="details" value="{{tipDetails}}">
        </div>
        <button type="submit" class="btn btn-primary">提交</button>
    </form>
</div>