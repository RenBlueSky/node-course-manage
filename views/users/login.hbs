{{#each errors}}
    <div class="alert alert-danger">{{errortip}}</div>
{{else}}

{{/each}}

<div class="row">
    <div class="col-md-6 mx-auto">
        <div class="card card-body">
            <img src="/img/logo.png" class="logo">
            <h3 class="text-center">登陆</h3>
            <form action="/users/login" method="post">
                <div class="form-group">
                    <label for="login_email">用户名</label>
                    <input type="text" name="login_name" class="form-control" value="name">
                </div>
                <div class="form-group">
                    <label for="login_password">密码</label>
                    <input type="password" name="login_password" class="form-control" value="password">
                </div>
                <button class="btn btn-primary">登陆</button>
            </form>
        </div>
    </div>
</div>